#!/usr/bin/env bash
# Universal Design Suite CLI Launcher for tailwindcss
# Automatically delegates to local binary or npx fallback

if command -v tailwindcss >/dev/null 2>&1; then
    exec tailwindcss ""
else
    exec npx -y @tailwindcss/cli ""
fi
