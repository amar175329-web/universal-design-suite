#!/usr/bin/env bash
# Universal Design Suite CLI Launcher for shadcn
# Automatically delegates to local binary or npx fallback

if command -v shadcn >/dev/null 2>&1; then
    exec shadcn ""
else
    exec npx -y shadcn@latest ""
fi
