#!/usr/bin/env bash
# Universal Design Suite CLI Launcher for tool-agent
# Automatically delegates to local binary or npx fallback

if command -v tool-agent >/dev/null 2>&1; then
    exec tool-agent ""
else
    exec npx -y tool-agent ""
fi
