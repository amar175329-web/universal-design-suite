#!/usr/bin/env bash
# Universal Design Suite CLI Launcher for create-assistant-ui
# Automatically delegates to local binary or npx fallback

if command -v create-assistant-ui >/dev/null 2>&1; then
    exec create-assistant-ui ""
else
    exec npx -y create-assistant-ui ""
fi
