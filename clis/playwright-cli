#!/usr/bin/env bash
# Universal Design Suite CLI Launcher for playwright-cli
# Automatically delegates to local binary or npx fallback

if command -v playwright-cli >/dev/null 2>&1; then
    exec playwright-cli ""
else
    exec npx -y @playwright/cli ""
fi
