#!/usr/bin/env bash
# Universal Design Suite CLI Launcher for lobe
# Automatically delegates to local binary or npx fallback

if command -v lobe >/dev/null 2>&1; then
    exec lobe ""
else
    exec npx -y @lobehub/cli ""
fi
